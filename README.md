# Apex Racing — Race Control

A live race-control interface for a Formula 1 race engineer. All data is simulated in the
browser; there is no backend.

## Running it

```sh
pnpm install
pnpm dev      # http://localhost:5173
```

Node 20+ and pnpm. `pnpm build` produces a static bundle in `dist/`.

Full documentation lands before submission.
